# Daily Stock Analysis Remaining Compatibility Debt Review (2026-04-13)

## 结论

当前仓库主线已经比较干净，但还残着一批**兼容层遗留**。这些残留大致分三类：

1. **明确是兼容壳，但涉及外部行为/配置，不适合我直接删**
2. **历史文档痕迹，技术上可清，但收益不高**
3. **纯工作区边角料，可低风险处理**

也就是说：

- **主线代码不需要再大动**
- 还能继续收，但下一刀已经开始碰到“外部接口/生产配置/兼容面”边界

---

## 本轮扫描范围

我对仓库内残留的以下关键词做了静态扫描：

- `webui`
- `bot`
- `feishu`
- `discord`
- `telegram`

同时补看了这些关键文件：

- `main.py`
- `docker/docker-compose.yml`
- `src/config.py`
- `src/notification.py`

---

## 一、明确存在，但不建议我直接删的兼容层

### 1) `main.py` 仍保留 `--webui` / `--webui-only`

位置：

- `main.py`

现状：

- `--webui` 仍作为 `--serve` 的历史兼容别名
- `--webui-only` 仍作为 `--serve-only` 的历史兼容别名

判断：

- 这不是死代码，而是**CLI 外部接口兼容层**
- 如果直接删，会改变历史调用命令的行为

结论：

- **不建议我在未确认前直接删除**
- 如果要删，应该作为一次明确的“CLI 兼容口径收缩”来做

---

### 2) `docker/docker-compose.yml` 仍使用 `WEBUI_HOST`

位置：

- `docker/docker-compose.yml`

现状：

- Compose 里仍写着：`WEBUI_HOST=0.0.0.0`
- 注释语义已经偏向 API 服务，但变量名还沿用旧口径

判断：

- 这是**部署/运行配置兼容面**
- 虽然技术上大概率能切到 `API_HOST`，但这已经属于生产配置口径调整

结论：

- **不建议我未经确认直接改**
- 如果要收这刀，建议和部署文档一起统一口径

---

### 3) `src/config.py` 里仍保留 `discord_bot_status`

位置：

- `src/config.py`

现状：

- 仍有 `discord_bot_status: str = "A股智能分析 | /help"`

判断：

- 这字段语义上已经偏 legacy
- 但它可能仍被：
  - 配置注册表
  - 旧测试
  - 兼容读取路径
  依赖

结论：

- 这是**配置兼容债**，不是无脑死字段
- **可以列入下一轮候选，但不适合盲删**

---

### 4) `src/notification.py` 仍保留完整渠道枚举与 no-op 发送壳

位置：

- `src/notification.py`

现状：

- `NotificationChannel` 仍包含 `FEISHU / TELEGRAM / DISCORD / SLACK ...`
- 各 `send_to_xxx()` 已全部退化为 `_log_send_disabled()`

判断：

- 这批不是活跃功能，而是**旧调用方兼容壳**
- 直接删会影响：
  - 旧测试
  - 旧导入路径
  - 旧调用方属性/方法访问

结论：

- 当前阶段保留是合理的
- 真要删，应该作为一次明确的**NotificationService API 收口**单独推进

---

## 二、可以继续清，但收益一般的历史文档残留

### 1) `docs/CHANGELOG.md` 大量保留 WebUI / Bot / Discord / Telegram 历史条目

位置：

- `docs/CHANGELOG.md`

判断：

- 这些内容多数是**历史记录**，不是当前产品定位说明
- 技术上不是错，但会让人搜索时感觉仓库还带完整 Bot/WebUI 产品面

结论：

- **如果目标是“现状导向文档收口”可以精简**
- **如果目标是保留完整演进历史，那就不该动**
- 这一刀更偏文档策略，不是代码问题

---

### 2) 英文文档中仍有旧 Bot/Feishu 入口链接痕迹

例如：

- `docs/README_EN.md` 里还看到 `bot/feishu-bot-config.md` 这类链接
- FAQ / full-guide 中仍有部分历史兼容表述

判断：

- 这批属于**文档口径未完全拔干净**
- 风险不高，但量会比较散

结论：

- 可作为一轮独立 docs cleanup
- 不建议和接口/配置调整混做

---

## 三、纯边角料，可低风险处理

### 1) 空目录残留

当前扫到：

- `.github/scripts`
- `.github/workflows`

判断：

- 这两个目录在当前仓库里已经是空壳
- 删除风险极低

备注：

- 这是物理删除动作；虽然风险低，但本质仍是删目录

---

### 2) 工作区 `__pycache__` 残留

现状：

- 仓库内（不含 `.venv`）仍有多处 `__pycache__`
- 这类目录已被 `.gitignore` 忽略，不影响版本控制

判断：

- 属于纯运行缓存
- 可以清，但只影响工作区整洁度，不产生架构价值

结论：

- 有空可以顺手清
- 不是值得单独开一刀的核心事项

---

## 建议的下一步顺序

如果继续推进，我建议按下面顺序，不要混着来：

### 方案 A：最稳
只做一轮**纯文档/兼容债盘点收口**，不改任何外部行为

可做内容：

- 补一份“当前仍保留的兼容层清单”
- 或清理英文文档里的明显失效链接/表述

### 方案 B：低风险清理
只清：

- 空目录
- `__pycache__`

优点：

- 风险最低

缺点：

- 工程价值有限，更像卫生打扫

### 方案 C：真正继续收官
开始处理兼容债，但这会触碰你之前要求我暂停确认的边界：

- 删 `--webui` / `--webui-only`
- 改 Compose 里的旧变量名
- 缩 `NotificationService` / `Config` 的旧兼容表面

这已经不是“边角清理”，而是**兼容口径收缩**。

---

## 我的判断

**现在最合理的状态不是继续盲删，而是到“兼容债是否继续收”的决策点了。**

因为真正还能收出价值的部分，已经开始碰：

- 外部 CLI 兼容
- 部署配置口径
- 旧服务 API 兼容面

这些都不该我默认替你拍板。
